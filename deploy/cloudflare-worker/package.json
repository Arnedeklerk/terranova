{
  "name": "terrascope-telemetry",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker receiving TerraScope opt-in telemetry. Stores events to Workers KV with 30-day raw retention per PRIVACY.md.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}
