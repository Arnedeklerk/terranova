# SAM-prompted segmentation

Phase 2 — placeholder.

End-state: pre-compute image embeddings per raster (Geo-SAM pattern), then segment buildings, fields, or water bodies in real time with text/point/box prompts using SAM 3.
